# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html

problem:
  subject: I have a problem
  content: My computer doesn't start anymore
  user_id: <%= ActiveRecord::Fixtures.identify(:bob) %>
  assignee_id: <%= ActiveRecord::Fixtures.identify(:alice) %>
  status_id: <%= ActiveRecord::Fixtures.identify(:open) %>

two:
  subject: Some problems
  content: Word doesn't open, internet doesn't work
  user_id: <%= ActiveRecord::Fixtures.identify(:bob) %>
  assignee_id: <%= ActiveRecord::Fixtures.identify(:alice) %>
  status_id: <%= ActiveRecord::Fixtures.identify(:closed) %>
