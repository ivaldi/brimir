<div class="row mt">
  <div class="medium-8 large-6 columns medium-centered">
    <div class="panel paxl">
      <h3 class="mb"><%= t(:forgot_password) %></h3>
      <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
        <%= f.email_field :email %>
        <%= f.submit t(:send_instructions), class: 'button radius mtm no-mb' %> <%= render "devise/shared/links" %>
      <% end %>
    </div>
  </div>
</div>
