<% unless params[:ticket][:status].blank? %>
  jQuery('[data-dropdown="statuses-<%= @ticket.id %>"]').text('<%= j(t(@ticket.status)) %>');

  <%# close all dropdowns %>
  var dropdowns = jQuery('.f-dropdown');
  dropdowns.foundation('dropdown', 'close', dropdowns);

<% else %>
  window.location.reload();
<% end %>

